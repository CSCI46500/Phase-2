# AWS Configuration Template
# Copy this file to .env.aws and fill in your actual values
# DO NOT commit .env.aws to git!

# AWS Configuration
AWS_REGION=us-east-1
PROJECT_NAME=model-registry
ENVIRONMENT=production

# ECR Repository Names
ECR_BACKEND_REPO=${PROJECT_NAME}-backend
ECR_FRONTEND_REPO=${PROJECT_NAME}-frontend

# RDS Database Configuration
DB_NAME=modelregistry
DB_USERNAME=admin
DB_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD  # Generate with: openssl rand -base64 32
DB_INSTANCE_CLASS=db.t3.micro  # Free tier eligible
DB_ALLOCATED_STORAGE=20

# S3 Bucket Configuration
# Bucket name will be: ${PROJECT_NAME}-models-${AWS_ACCOUNT_ID}
S3_BUCKET_NAME=${PROJECT_NAME}-models-REPLACE_WITH_YOUR_ACCOUNT_ID

# ECS Configuration
ECS_CLUSTER_NAME=${PROJECT_NAME}-cluster
ECS_BACKEND_SERVICE=${PROJECT_NAME}-backend-service
ECS_FRONTEND_SERVICE=${PROJECT_NAME}-frontend-service

# Admin User (Required by specification)
ADMIN_USERNAME=ece30861defaultadminuser
ADMIN_PASSWORD='correcthorsebatterystaple123(!__+@**(A'"'"'";DROP TABLE packages;'

# Security
SECRET_KEY=CHANGE_ME_TO_RANDOM_SECRET  # Generate with: openssl rand -hex 32

# Optional: API Keys for external services
ANTHROPIC_API_KEY=
GITHUB_TOKEN=
HF_TOKEN=  # HuggingFace API token for model downloads

# CloudWatch Configuration
LOG_RETENTION_DAYS=7

# Auto-scaling Configuration (optional)
ECS_MIN_TASKS=1
ECS_MAX_TASKS=4
ECS_TARGET_CPU_PERCENT=70

# Notes:
# 1. Get your AWS Account ID: aws sts get-caller-identity --query Account --output text
# 2. The S3_BUCKET_NAME should include your account ID for uniqueness
# 3. Generate secure passwords for DB_PASSWORD and SECRET_KEY using the commands above
# 4. Never commit .env.aws to version control - add it to .gitignore
